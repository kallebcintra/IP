// Not Done
#include <stdio.h>

#define l 999999999

int main()
{
    int M[l], N[l];
    int i, j;

    while(M != 0 && N != 0)
    {
        scanf("%d %d", M, N);
    }

    return 0;
}